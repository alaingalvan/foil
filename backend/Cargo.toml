[package]
name = "foil_backend"
version = "0.5.5"
authors = ["Alain Galvan <hi@alain.xyz>"]
edition = "2024"

[dependencies]
async-graphql = { version = "7.2.x", features = [
    "dataloader",
    "decimal",
    "chrono",
    "uuid",
] }
async-graphql-axum = "7.2.x"
axum = "0.8.x"
axum-macros = "0.5.x"
chrono = { version = "0.4.x", features = ["serde"] }
env_logger = "0.11.x"
glob = "0.3.x"
hyper-util = { version = "0.1.x", features = ["full"] }
lexiclean = "0.0.2"
log = "0.4.x"
path-slash = "0.2.x"
regex = "1.x.x"
serde = "1.0.x"
sqlx = { version = "0.9.x", features = [
    "chrono",
    "json",
    "postgres",
    "regexp",
    "runtime-tokio",
    "time",
    "uuid",
] }
tokio = { version = "1.53.x", features = ["full"] }
tower = { version = "0.5.x", features = [
    "limit",
    "load-shed",
    "timeout",
    "util",
] }
tower-http = { version = "0.7.x", features = [
    "compression-gzip",
    "fs",
    "trace",
] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 16
strip = true