# Foil Frontend Builder

Bundles your application, watches for changes, and sets up your frontend application runtime.

```bash
foil-builder --root-path <path> --system --public-modules <list> --vendor --input <main> --output <dir>
```